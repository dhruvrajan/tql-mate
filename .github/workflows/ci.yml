name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  TYPEDB_URL: typedb://admin:password@127.0.0.1:1729/tqlmate_ci

jobs:
  unit:
    name: unit
    runs-on: depot-ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Offline unit tests (no TypeDB / no Docker)
        run: >
          cargo test --no-default-features
          --test url --test migration --test ledger --test cli

  lint:
    name: lint
    runs-on: depot-ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: rustfmt
        run: cargo fmt --all -- --check
      - name: clippy
        run: cargo clippy --all-targets --features typedb-docker -- -D warnings

  integration:
    name: integration
    needs: unit
    runs-on: depot-ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: TypeDB via testcontainers
        run: >
          cargo test --features typedb-docker --test typedb_docker
          -- --nocapture --test-threads=1

  scala-test:
    name: scala unit + integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: coursier/cache-action@v6
      - uses: VirtusLab/scala-cli-setup@v2
        with:
          jvm: "temurin:21"
          power: true
      - name: Setup TypeDB
        uses: typedb/setup-typedb@1.0.0
        with:
          typedb_version: "3.12.3"
          typedb_grpc_port: "1729"
      - name: Wait for TypeDB
        working-directory: scala
        run: scala-cli run . -- wait --timeout 120 -u "${TYPEDB_URL}"
      - name: Tests
        working-directory: scala
        run: scala-cli test .
        env:
          TQLMATE_REQUIRE_TYPEDB: "1"

  scala-native-image:
    name: scala native-image (linux amd64)
    needs: scala-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: coursier/cache-action@v6
      - uses: VirtusLab/scala-cli-setup@v2
        with:
          jvm: "temurin:21"
          power: true
      - name: Setup TypeDB
        uses: typedb/setup-typedb@1.0.0
        with:
          typedb_version: "3.12.3"
          typedb_grpc_port: "1729"
      - name: Package native-image
        working-directory: scala
        run: |
          scala-cli --power package --native-image . \
            --force -o tqlmate-scala \
            --graalvm-args --no-fallback \
            --graalvm-args --enable-url-protocols=http,https \
            --graalvm-args -H:+ReportExceptionStackTraces
          ./tqlmate-scala --help
          ./tqlmate-scala -u "${TYPEDB_URL}" create
          ./tqlmate-scala -u "${TYPEDB_URL}" status
          ./tqlmate-scala -u "${TYPEDB_URL}" drop
        env:
          TYPEDB_URL: typedb://admin:password@127.0.0.1:1729/tqlmate_scala_ni
      - uses: actions/upload-artifact@v4
        with:
          name: tqlmate-scala-x86_64-linux
          path: scala/tqlmate-scala

  rust-build:
    needs: unit
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: depot-ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - os: depot-ubuntu-24.04
            target: aarch64-unknown-linux-gnu
            cross: true
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: windows-latest
            target: aarch64-pc-windows-msvc
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked
      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: tqlmate-${{ matrix.target }}
          path: |
            target/${{ matrix.target }}/release/tqlmate
            target/${{ matrix.target }}/release/tqlmate.exe
